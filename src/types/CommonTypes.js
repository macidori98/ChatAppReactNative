import React from 'react';

/**
 * @template T
 * @typedef {[T, React.Dispatch<React.SetStateAction<T>>]} UseState
 */

/**
 * @template T
 * @typedef {React.MutableRefObject<T>} UseRef
 */

export {};
